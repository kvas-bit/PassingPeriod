# Agent architecture (local AI context — never commit)
.claude/
.cursor/
CLAUDE.md
AGENTS.md
GEMINI.md
MEMORY/
STATE/
TEMPLATES/
.planning/
CURSOR_HANDOFF.md
.superpowers/
docs/superpowers

# macOS
.DS_Store

# Git worktrees (local parallel checkouts — never commit)
.worktrees/

# Xcode / local build products (never commit)
.derivedData/

# Xcode user-specific data (machine-specific — never commit)
**/xcuserdata/

# Xcode workspace UI state directories
**/*.xcworkspacedata/
